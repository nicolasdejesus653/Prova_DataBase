services:
  db:
    image: postgres:alpine
    container_name: db_prova
    environment:
      POSTGRES_USER: prova_ads
      POSTGRES_PASSWORD: ads2026
      POSTGRES_DB: ads_app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
